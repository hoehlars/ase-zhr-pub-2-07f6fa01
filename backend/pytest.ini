[pytest]
# backend/ auf den Pfad legen, damit "import app" funktioniert; tests/ wird von
# pytest automatisch ergänzt, sodass "from conftest import ..." klappt.
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
